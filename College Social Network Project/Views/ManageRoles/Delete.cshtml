@model College_Social_Network_Project.Models.AspNetRole

@{
    ViewBag.Title = "Delete";
}

<link href="~/Content/StyleSheet2.css" rel="stylesheet" />
<h1><br></h1>

<h3 id="Duser">Are you sure you want to delete this?</h3>
<div>

    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>

    <div class="form">
        <section class="loginForm">
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-group">
                    <div class="col-md-offset-4 col-md-3">
                        <input type="submit" value="Delete" class="button-55" id="delete" />
                        <br /><br />
                    </div>

                    <div class="col-md-offset-4 col-md-5">
                        @Html.ActionLink("Cancel", "Index", null, new { @class = "button-55" })
                        <br /><br />
                    </div>
                </div>
            }
        </section>
    </div>

</div>
